# ComfyNexus Backend Test Dependencies
# Python 3.12.x
#
# 重要提示：
# 测试依赖应该安装在独立的测试环境中，不要污染开发环境。
# 
# 推荐方式 1：使用独立的测试虚拟环境
#   python -m venv .venv-test
#   .venv-test\Scripts\activate
#   pip install -r requirements.txt
#   pip install -r requirements-test.txt
#
# 推荐方式 2：使用 tox 管理测试环境
#   pip install tox
#   tox
#
# 不推荐：直接在开发环境安装测试依赖
#   这会污染开发环境，增加依赖冲突风险

# Testing Framework
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0

# Property Testing
hypothesis>=6.98.0

# Parallel Test Execution
pytest-xdist>=3.5.0

# Test Utilities
pytest-timeout>=2.2.0
